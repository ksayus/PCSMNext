<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:PCSMNext.App.Controls">
    <Style TargetType="{x:Type local:MyIconButton}">
        <Setter Property="Background" Value="{DynamicResource ColorButtonBg1}"/>
        <Setter Property="Foreground" Value="{DynamicResource ColorTextWhite}"/>
        <Setter Property="RenderTransformOrigin" x:Name="renderTransformOrigin" Value="0.5, 0.5"/>
        <Setter Property="Margin" Value="1"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="CornerRadius" Value="9"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Width" Value="120"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MyIconButton}">
                    <Grid Margin="{TemplateBinding Margin}"
                          RenderTransformOrigin="{TemplateBinding RenderTransformOrigin}">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ScaleTransform"/>
                            </Border.RenderTransform>

                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                <TextBlock x:Name="Icon"
                                           Text="{TemplateBinding IconText}"
                                           FontFamily="Segoe MDL2 Assets"
                                           Foreground="{TemplateBinding Foreground}"
                                           Margin="0,0,5,0"/>
                                <ContentPresenter x:Name="ContentElement"
                                                  Content="{TemplateBinding Content}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ColorButtonOver1}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ColorButtonPressed1}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ColorButtonEnabled1}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>